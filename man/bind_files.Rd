% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bind_files.R
\name{bind_files}
\alias{bind_files}
\title{Row-bind a list of data frames}
\usage{
bind_files(data, .id = "source", .guess = FALSE)
}
\arguments{
\item{data}{A list of data frames or tibbles.}

\item{.id}{Name of the source column added to identify which data frame
each row originated from. Default is \code{"source"}.}

\item{.guess}{If \code{TRUE}, all columns are coerced to character before
binding and then re-typed automatically. Use when column types differ
across files. Default is \code{FALSE}.}
}
\value{
A single tibble with all rows combined and a \code{source} column
(or whatever \code{.id} names it).
}
\description{
Combines a list of data frames into a single tibble using
\code{\link[purrr:list_rbind]{purrr::list_rbind()}}. When column types differ across files,
an informative error is raised with hints for resolution.
Setting \code{.guess = TRUE} converts all columns to character first, then
lets \code{\link[readr:type_convert]{readr::type_convert()}} infer a common type.
}
\examples{
df1 <- data.frame(x = 1:3, y = letters[1:3])
df2 <- data.frame(x = 4:5, y = letters[4:5])
bind_files(list(a = df1, b = df2))

# Force type guessing when types differ:
df3 <- data.frame(x = c("1", "2"))
df4 <- data.frame(x = c(3L, 4L))
bind_files(list(df3, df4), .guess = TRUE)

}
\seealso{
\code{\link[=view_types]{view_types()}} to inspect type differences before binding
}
