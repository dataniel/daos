% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_last_df.R
\name{track_last_df}
\alias{track_last_df}
\title{Track the last data frame printed in the console}
\usage{
track_last_df(on = TRUE, name = ".last.df")
}
\arguments{
\item{on}{\code{TRUE} to enable tracking (default), \code{FALSE} to disable.}

\item{name}{Name of the variable written in \code{.GlobalEnv}. Default:
\code{".last.df"}.}
}
\value{
\code{TRUE} invisibly.
}
\description{
Installs a task callback that automatically saves any data frame returned
to the console as a named variable in the global environment. Intermediate
expressions (\code{1 + 1}, plots, etc.) are ignored — only top-level data frame
returns are captured.
}
\details{
Calling \code{track_last_df()} again replaces any existing callback, preventing
duplicates.
}
\examples{
\dontrun{
track_last_df()          # enable
dplyr::starwars |> head()
.last.df                 # the last printed data frame

track_last_df(FALSE)     # disable
}

}
