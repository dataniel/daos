% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{task_step}
\alias{task_step}
\title{Run a production step under a task}
\usage{
task_step(db, id, expr, note = NULL)
}
\arguments{
\item{db}{Path to the task database, or an open DBI connection.}

\item{id}{Task identifier: the integer id, the uuid, or the key.}

\item{expr}{The step to run. Evaluated once; its value is returned.}

\item{note}{Optional success note. Defaults to a timestamped \code{"ok: ..."}.}
}
\value{
The value of \code{expr}, invisibly. On error, the error is re-raised
after the task is annotated and un-started.
}
\description{
Wraps one step of a production script so it both runs and records
itself: the task is marked started, \code{expr} is evaluated, and then -- on
success -- annotated and completed; on failure it is annotated with the
error, un-started, and the error re-raised. This turns the hand-rolled
\code{run_step()} pattern into one call, so a pipeline leaves a trace of what
happened without status code creeping into the analysis.
}
\details{
Keep it \emph{beside} the work: \code{expr} should be the step that produces the
result, with the task call wrapping it, not task code woven into a data
pipeline.
}
\examples{
\dontrun{
task_step(db, "compile-accounts", {
  stats <- compile(sources)
  stats
})
}

}
\seealso{
\code{\link[=task_start]{task_start()}}, \code{\link[=task_require]{task_require()}}, \code{\link[=task_cycle]{task_cycle()}}
}
