% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_signs.R
\name{find_signs}
\alias{find_signs}
\title{Find sign combinations that sum to a target}
\usage{
find_signs(
  df,
  label_col,
  value_col,
  total_label = "total",
  positive = NULL,
  negative = NULL,
  by = NULL,
  max_zeros = 2L
)
}
\arguments{
\item{df}{A data frame containing labels and values.}

\item{label_col}{Column containing item labels (unquoted).}

\item{value_col}{Column containing numeric values (unquoted).}

\item{total_label}{Label of the row whose value is the reconciliation
target. Default: \code{"total"}.}

\item{positive}{Character vector of labels that must receive a positive
sign. Optional.}

\item{negative}{Character vector of labels that must receive a negative
sign. Optional.}

\item{by}{Optional grouping columns (unquoted). When supplied, the
reconciliation is performed separately for each group.}

\item{max_zeros}{Maximum number of zero coefficients allowed in a valid
solution (i.e. how many items can be excluded). Default: \code{2L}.}
}
\value{
A tibble with the same label/value structure as the input, where
values have been multiplied by their resolved signs. Groups with no
unique solution are silently dropped (\code{NULL}).
}
\description{
Given a set of labelled values and a target total, determines which
multipliers (\code{+1}, \code{-1}, or \code{0}) to apply to each value so that the
signed sum equals the target. Uses a \emph{meet-in-the-middle} algorithm for
efficiency.
}
\details{
This is useful for reconciling accounting line items where the sign
convention is unknown, for example finding which items in a set of
account balances add up to a reported total.
}
\examples{
items <- data.frame(
  label = c("revenue", "costs", "depreciation", "total"),
  value = c(100, 40, 10, 50)
)
find_signs(items, label, value, total_label = "total")

}
