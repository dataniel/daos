% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_duplicates.R
\name{flag_duplicates}
\alias{flag_duplicates}
\title{Flag duplicate rows}
\usage{
flag_duplicates(data, ...)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{...}{Columns used to identify duplicates (tidy-select). If omitted,
all columns are used.}
}
\value{
A tibble with \code{isdup} and \code{dupid} prepended.
}
\description{
Prepends two columns to a data frame: \code{isdup} (logical, \code{TRUE} when the row
appears more than once) and \code{dupid} (integer group identifier, \code{0} for
unique rows). Uses \code{data.table} internally for speed.
}
\examples{
# All columns
flag_duplicates(ggplot2::mpg)

# Specific columns
flag_duplicates(ggplot2::mpg, manufacturer, model, year)

# Combined with expect_empty() for a validation pipeline:
\dontrun{
ggplot2::mpg |>
  flag_duplicates() |>
  dplyr::filter(isdup) |>
  expect_empty(warn_msg = "Duplicate rows found")
}

}
\seealso{
\code{\link[=expect_empty]{expect_empty()}}
}
