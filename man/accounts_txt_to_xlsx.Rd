% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accounts_txt_to_xlsx.R
\name{accounts_txt_to_xlsx}
\alias{accounts_txt_to_xlsx}
\title{Parse formatted text files and export to Excel}
\usage{
accounts_txt_to_xlsx(txt_dir, out_file, year, min_spaces = 3)
}
\arguments{
\item{txt_dir}{Path to the directory containing \code{.txt} files.}

\item{out_file}{Path to the output \code{.xlsx} file.}

\item{year}{The accounting year as a numeric scalar (e.g. \code{2024}).}

\item{min_spaces}{Minimum number of consecutive spaces used as field
delimiter. Default \code{3}.}
}
\value{
The parsed data as a tibble, invisibly.
}
\description{
Reads all \code{.txt} files in a directory, parses them according to a specific
layout used for manually formatted financial statements, and exports the
result as an Excel file. Messages report how many files were collected and
a summary when done. Files are validated one company at a time, so a
formatting problem aborts at the offending company with a message naming
the CVR, the file, and the offending lines or elements.
}
\details{
\strong{Text file format:}

Each line is either a \emph{category line} or a \emph{data line}:
\itemize{
\item \strong{Category line:} a single string with no field delimiter. Becomes the
\code{note} column for all subsequent data lines.
\item \strong{Data line:} three fields separated by \code{min_spaces} or more consecutive
spaces: (1) element name, (2) amount for \code{year}, (3) amount for \code{year - 1}.
}

Amounts must be in whole kroner (periods as thousands separators are stripped
automatically). If the previous year is absent, the third field may be empty
-- it becomes \code{NA}.

Appending \code{ statnatio} to a category line negates all values in that
category (useful when costs appear with a positive sign in notes).

File names are used as identifiers in the \code{cvr} column. A trailing \verb{_spec}
suffix is stripped automatically (e.g. \verb{12345678_spec.txt} -> \code{12345678}).
}
\examples{
\dontrun{
accounts_txt_to_xlsx("data/txt", "data/output.xlsx", year = 2024)
}

}
