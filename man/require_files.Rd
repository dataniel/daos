% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/require_files.R
\name{require_files}
\alias{require_files}
\title{Validate and resolve file paths}
\usage{
require_files(paths, .names = NULL, .envir = parent.frame())
}
\arguments{
\item{paths}{A character vector of file paths. Glue syntax (\code{{}}) is
supported for compact range expansion.}

\item{.names}{Optional character vector of names to assign to the returned
paths. Defaults to the file name without extension.}

\item{.envir}{Environment used for glue interpolation. Default is the
calling frame.}
}
\value{
A named character vector of validated file paths.
}
\description{
Expands paths using \code{\link[glue:glue]{glue::glue()}} (so \code{{0:9}} generates ten paths),
checks that every file exists, and returns a named character vector ready
to pipe into \code{\link[=read_files]{read_files()}}. Aborts immediately if any file is missing.
}
\examples{
\dontrun{
# Validate ten files at once using glue expansion:
require_files("data/dat{0:9}.parquet")

# Pipe directly into read_files():
require_files("data/dat{0:9}.parquet") |> read_files()

# Full pipeline: validate -> read -> bind:
require_files("data/dat{0:9}.parquet") |>
  read_files() |>
  bind_files()
}

}
\seealso{
\code{\link[=read_files]{read_files()}}, \code{\link[=bind_files]{bind_files()}}
}
