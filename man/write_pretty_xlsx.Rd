% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_pretty_xlsx.R
\name{write_pretty_xlsx}
\alias{write_pretty_xlsx}
\title{Write data frames to an xlsx file}
\usage{
write_pretty_xlsx(
  data = NULL,
  path,
  append = NULL,
  overwrite = FALSE,
  as_table = FALSE,
  freeze_header = TRUE,
  skip_fmt = NULL
)
}
\arguments{
\item{data}{A data frame or named list of data frames. Creates a new file
(overwrites if it already exists). A single data frame defaults to sheet
name \code{"Sheet1"}; unnamed list elements get names \code{"Sheet1"}, \code{"Sheet2"},
etc.}

\item{path}{Path to the output \code{.xlsx} file.}

\item{append}{A data frame or named list of data frames to add as new sheets
to an existing file. Same naming defaults as \code{data}. Requires that \code{path}
exists.}

\item{overwrite}{If \code{FALSE} (default), aborts when a sheet supplied via
\code{append} already exists in the workbook. Set to \code{TRUE} to replace it.}

\item{as_table}{If \code{TRUE}, data is inserted as an Excel ListObject (table
with filter arrows and banded rows). Default \code{FALSE}.}

\item{freeze_header}{If \code{TRUE} (default), freezes the first row.}

\item{skip_fmt}{Character vector of column names to exclude from the
\verb{#,##0} number format.}
}
\value{
\code{path}, invisibly.
}
\description{
Creates or updates an Excel workbook. \code{data} creates/overwrites the file;
\code{append} adds sheets to an existing file. All numeric columns are formatted
with a thousand separator and no displayed decimals (\verb{#,##0}) when the
column contains at least one value >= 1000; underlying values are preserved. \code{NA} values are shown as blank cells. The header row
is bold, and the first row is frozen by default.
}
\examples{
\dontrun{
# New file with one sheet
write_pretty_xlsx(mtcars, "out.xlsx")

# New file with multiple sheets
write_pretty_xlsx(list(Cars = mtcars, Iris = iris), "out.xlsx")

# Append a sheet to an existing file
write_pretty_xlsx(append = list(Extra = airquality), path = "out.xlsx")

# Create file and append in one call
write_pretty_xlsx(list(Hoved = mtcars), "out.xlsx", append = list(Bilag = iris))

# Insert as Excel table
write_pretty_xlsx(mtcars, "out.xlsx", as_table = TRUE)
}

}
