% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cvr.R
\name{cvr_hits}
\alias{cvr_hits}
\title{Extract CVR search hits as a tibble}
\usage{
cvr_hits(response)
}
\arguments{
\item{response}{A parsed response list from \code{\link[=cvr_search]{cvr_search()}}.}
}
\value{
A tibble with one row per document. Warns if the \code{cvrnummer} or
\code{dokumenturl} columns needed by \code{\link[=cvr_download]{cvr_download()}} are missing.
}
\description{
Flattens the hits in a response from \code{\link[=cvr_search]{cvr_search()}} into a tibble with one
row per document. Nested field names are reduced to their last component
and lowercased (e.g. \code{dokumenter.dokumentUrl} becomes \code{dokumenturl}).
Fields with several values per hit (typically multiple documents) become
multiple rows; single-valued fields are recycled across them.
}
\examples{
\dontrun{
hits <- cvr_query("12345678", "2024-01-01", "2024-12-31") |>
  cvr_search(contact = "you@example.com") |>
  cvr_hits()
}

}
\seealso{
\code{\link[=cvr_query]{cvr_query()}}, \code{\link[=cvr_search]{cvr_search()}}, \code{\link[=cvr_download]{cvr_download()}}

Other cvr:
\code{\link[=cvr_download]{cvr_download()}},
\code{\link[=cvr_query]{cvr_query()}},
\code{\link[=cvr_search]{cvr_search()}}
}
\concept{cvr}
\keyword{internal}
