% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{task_start}
\alias{task_start}
\alias{task_stop}
\title{Start or stop work on a task}
\usage{
task_start(db, id)

task_stop(db, id)
}
\arguments{
\item{db}{Path to the task database, or an open DBI connection.}

\item{id}{Task identifier: the integer id, the uuid, or the key.}
}
\value{
\code{TRUE}, invisibly.
}
\description{
\code{task_start()} marks a pending task as in progress by stamping it with a
start time; \code{task_stop()} clears that stamp. A started task stays
\code{pending} -- "in progress" is a flag, not a separate status -- and
\code{\link[=task_list]{task_list()}} reports it through the \code{started} column. This is what lets
the overview show \emph{what is being worked on right now}, not just what is
pending, so a production step can announce itself when it begins:
\code{task_start(db, "compile-accounts")} at the top of the step, beside the
work rather than inside it.
}
\examples{
\dontrun{
task_start(db, "compile-accounts")   # now shown as in progress
task_stop(db, "compile-accounts")    # back to plain pending
}

}
\seealso{
\code{\link[=task_step]{task_step()}}, \code{\link[=task_done]{task_done()}}, \code{\link[=task_list]{task_list()}}
}
