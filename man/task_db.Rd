% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{task_db}
\alias{task_db}
\title{Create or open a shared task database}
\usage{
task_db(path)
}
\arguments{
\item{path}{Path to the \code{.sqlite} file.}
}
\value{
\code{path}, invisibly.
}
\description{
Initialises a SQLite task database (creating the file and tables if they
do not exist) and switches it to WAL mode, so several R processes can
read and write the same file concurrently. Safe to call repeatedly.
}
\details{
SQLite is free (public domain) and bundled by the \code{RSQLite} package, so
there is no server to run and nothing to license -- a shared \code{.sqlite}
file on a network drive is enough for a team to work from.
}
\examples{
\dontrun{
task_db("tasks.sqlite")
}

}
\seealso{
\code{\link[=task_add]{task_add()}}, \code{\link[=task_list]{task_list()}}, \code{\link[=task_app]{task_app()}}
}
