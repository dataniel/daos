% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unpack_files.R
\name{unpack_files}
\alias{unpack_files}
\title{Unpack a named list into individual variables}
\usage{
unpack_files(data, .envir = parent.frame(), .overwrite = FALSE)
}
\arguments{
\item{data}{A fully named list.}

\item{.envir}{Target environment. Default is the calling environment.}

\item{.overwrite}{If \code{FALSE} (default), aborts when any name already
exists in the target environment. Set to \code{TRUE} to allow overwrites.}
}
\value{
\code{data} invisibly.
}
\description{
Assigns each element of a named list to its own variable in a target
environment. This is the complement of \code{\link[=read_files]{read_files()}} when you want
individual named objects rather than a single list.
}
\examples{
\dontrun{
# Read files into separate variables:
require_files("data/dat{0:9}.parquet") |>
  read_files() |>
  unpack_files()

# dat0, dat1, ..., dat9 now exist in the environment.
# Collect them again:
summon("^dat\\\\d+$")
}

}
\seealso{
\code{\link[=read_files]{read_files()}}, \code{\link[=summon]{summon()}}
}
