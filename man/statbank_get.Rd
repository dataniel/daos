% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statbank.R
\name{statbank_get}
\alias{statbank_get}
\title{Download a table from a statbank}
\usage{
statbank_get(
  table,
  ...,
  lang = NULL,
  bank = "gl",
  .col_names = c("text", "code"),
  .values = c("text", "code"),
  .type_convert = TRUE
)
}
\arguments{
\item{table}{The table's path in the tree, e.g.
\code{"BE/BE01/BEXSAT1.PX"}. A bare table id (e.g. \code{"BEXSAT1.PX"}) also
works once the table list has been fetched with \code{\link[=statbank_tables]{statbank_tables()}}
or \code{\link[=statbank_search]{statbank_search()}} in the same session.}

\item{...}{Named selections, one per variable. Values can be value
codes, value texts, or \code{"*"} for all. Because the option arguments
are dot-prefixed, a variable can never be shadowed by one of them.}

\item{lang}{Language of titles and labels, or \code{NULL} (default) for the
bank's own default. Greenland offers \code{"da"}, \code{"kl"}, \code{"en"}; the
Faroe Islands offer \code{"fo"}, \code{"en"}.}

\item{bank}{Which statbank: \code{"gl"} (Greenland, the default) or \code{"fo"}
(the Faroe Islands).}

\item{.col_names}{\code{"text"} (default) names the columns by the
variables' display texts; \code{"code"} uses the variable codes.}

\item{.values}{\code{"text"} (default) fills the cells with value display
texts; \code{"code"} uses the value codes.}

\item{.type_convert}{If \code{TRUE} (default), the result is passed
through \code{\link[readr:type_convert]{readr::type_convert()}}, so e.g. a year column comes back
numeric. Set to \code{FALSE} to keep every variable column as character.}
}
\value{
A tibble with one column per variable and a \code{value} column.
The table's footnotes and provenance ride along as attributes:
\code{attr(df, "notes")}, \code{attr(df, "source")}, \code{attr(df, "updated")},
and \code{attr(df, "contact")}.
}
\description{
Fetches data for a table and returns it as a tibble in long format:
one column per variable and a \code{value} column with the numbers.
}
\details{
Selections are passed as named arguments. Names are matched against
the variable codes \emph{and} their display texts, and values against both
value codes and value texts, so
\code{statbank_get("BE/BE01/BEXSAT1.PX", tid = 2024, art = "Antal")} works
without knowing the internal codes. Matching ignores case and folds
Danish letters (\code{foedested} matches \verb{f\\u00f8dested}). Use \code{"*"} to select
all values; variables that are not mentioned default to all values.

By default, columns are named by the variables' display texts in the
chosen language, and cells hold the display texts of the values. Set
\code{.col_names = "code"} and/or \code{.values = "code"} to get the internal
codes instead, e.g. when a coded column (such as sex as 0/1) is
wanted for joins or modelling. The option arguments are dot-prefixed
so they can never collide with a variable name in \code{...}.
}
\examples{
\dontrun{
df <- statbank_get(
  "BE/BE01/BEXSAT1.PX",
  tid = c(2023, 2024, 2025),
  art = "Antal"
)
attr(df, "notes")

# Codes instead of display texts, no type conversion:
df <- statbank_get(
  "BE/BE01/BEXSAT1.PX",
  tid = 2024,
  .col_names = "code",
  .values = "code",
  .type_convert = FALSE
)
}

}
\seealso{
\code{\link[=statbank_meta]{statbank_meta()}}, \code{\link[=statbank_search]{statbank_search()}}, \code{\link[=statbank_app]{statbank_app()}}
}
