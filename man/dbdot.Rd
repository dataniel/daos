% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbdot.R
\name{dbdot}
\alias{dbdot}
\title{Format DB07 industry codes with dots}
\usage{
dbdot(x)
}
\arguments{
\item{x}{A character vector of DB07 codes. Numeric vectors are
converted with \code{as.character()} first.}
}
\value{
A character vector of the same length.
}
\description{
Danish DB07 industry codes (Dansk Branchekode 2007) appear in data both
with and without dots: \code{"011100"} in one register, \code{"01.11.00"} in a
classification table. \code{dbdot()} normalises to the dotted form by
stripping any existing dots and spaces and inserting a dot after every
second digit. It works at any aggregation level (\code{"01.1"}, \code{"01.11"},
\code{"01.11.00"}) and is idempotent, so mixed input comes out uniform.
}
\details{
The inverse (the bare-digit form) is simply
\code{gsub("[.]", "", x)}, so it has no function of its own.

No digits are invented: a code that lost its leading zero in Excel
(e.g. \code{11100} for \code{"011100"}) cannot be repaired safely, because a
3- or 5-digit code is ambiguous between aggregation levels. Keep
industry codes as character columns to avoid the problem at the
source.
}
\examples{
dbdot(c("011100", "01.1100", "0111", "011", NA))

# Typical use: match register data to a classification table
# df |> dplyr::mutate(branche = dbdot(branche))

}
\seealso{
\code{\link[=clean_cpr]{clean_cpr()}}, \code{\link[=clean_cvr]{clean_cvr()}}
}
