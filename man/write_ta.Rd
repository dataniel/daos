% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_ta.R
\name{write_ta}
\alias{write_ta}
\title{Write a Greenlandic TA file}
\usage{
write_ta(x, path)
}
\arguments{
\item{x}{A data frame with exactly the columns \code{nrnr}, \code{trans}, \code{brch},
\code{bas}, \code{eng}, \code{det}, \code{afg}, and \code{kbx}. \code{moms} is optional; if absent it
is derived automatically (see Details). Any other columns cause an error.}

\item{path}{Path to write to.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes a data frame to a Greenlandic TA fixed-width file. Writes the nine
columns \code{nrnr}, \code{trans}, \code{brch}, \code{bas}, \code{eng}, \code{det}, \code{afg}, \code{moms}, and
\code{kbx}. Numeric columns are written without decimal places; character
columns are left-aligned in their field.
}
\details{
If \code{moms} is absent from \code{x}, it is derived automatically: \code{NA} for rows
where \code{trans} is \code{"0100"} or \code{"0700"} (no VAT in Greenland), \code{0} otherwise.
}
\examples{
\dontrun{
write_ta(df, "ta.file")
}

}
