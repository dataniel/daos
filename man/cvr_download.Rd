% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cvr.R
\name{cvr_download}
\alias{cvr_download}
\title{Download CVR documents}
\usage{
cvr_download(hits, path, sleep = 3, overwrite = FALSE)
}
\arguments{
\item{hits}{A tibble with \code{cvrnummer} and \code{dokumenturl} columns, typically
from \code{\link[=cvr_hits]{cvr_hits()}}.}

\item{path}{Directory to download into. Created automatically if it does
not exist.}

\item{sleep}{Seconds to wait between downloads. Must be at least 1; the
default of 3 matches the API's expectations for bulk retrieval.}

\item{overwrite}{If \code{FALSE} (default), the function aborts when any of the
files it would write already exist. Set to \code{TRUE} to replace them.}
}
\value{
Invisibly, a character vector of paths to the downloaded files
(failed downloads are not included).
}
\description{
Downloads the documents listed in a tibble from \code{\link[=cvr_hits]{cvr_hits()}}, one file per
row, named by CVR number (repeated CVR numbers get a \verb{_2}, \verb{_3}, ...
suffix). A delay between requests keeps the load on the API polite.
}
\examples{
\dontrun{
cvr_query("12345678", "2024-01-01", "2024-12-31") |>
  cvr_search(contact = "you@example.com") |>
  cvr_hits() |>
  cvr_download("data/pdf")
}

}
\seealso{
\code{\link[=cvr_query]{cvr_query()}}, \code{\link[=cvr_search]{cvr_search()}}, \code{\link[=cvr_hits]{cvr_hits()}},
\code{\link[=accounts_pdf_to_txt]{accounts_pdf_to_txt()}} for the next step of the workflow.

Other cvr:
\code{\link[=cvr_hits]{cvr_hits()}},
\code{\link[=cvr_query]{cvr_query()}},
\code{\link[=cvr_search]{cvr_search()}}
}
\concept{cvr}
