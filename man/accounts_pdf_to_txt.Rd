% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accounts_pdf_to_txt.R
\name{accounts_pdf_to_txt}
\alias{accounts_pdf_to_txt}
\title{Convert PDF files to text files}
\usage{
accounts_pdf_to_txt(pdf_dir, txt_dir, overwrite = FALSE)
}
\arguments{
\item{pdf_dir}{Path to the directory containing PDF files.}

\item{txt_dir}{Path to the directory where text files will be written.
Created automatically if it does not exist.}

\item{overwrite}{If \code{FALSE} (default), the function aborts when any of the
\code{.txt} files it would write already exist. Set to \code{TRUE} to replace them.}
}
\value{
Invisibly, a character vector of paths to the written \code{.txt} files
(skipped PDFs are not included).
}
\description{
Reads all PDF files in a directory, extracts their text content using
\code{\link[pdftools:pdf_text]{pdftools::pdf_text()}}, and writes one \code{.txt} file per PDF to the output
directory. A progress bar is shown while converting; messages are only
emitted for skipped PDFs, plus a summary when done.
}
\details{
PDFs with no extractable text (blank or whitespace only, typically
scanned/photo-printed annual reports) are skipped with a warning message;
no \code{.txt} file is written for them.
}
\examples{
\dontrun{
accounts_pdf_to_txt("data/pdf", "data/txt")
}

}
