% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen_timeseries.R
\name{screen_timeseries}
\alias{screen_timeseries}
\title{Interactive time-series screening dashboard}
\usage{
screen_timeseries(
  data,
  x,
  y,
  series = NULL,
  .exclude = NULL,
  .title = NULL,
  .y_min = NULL,
  .y_max = NULL
)
}
\arguments{
\item{data}{A data frame containing at minimum an x-axis column, a y-axis
column, and at least one grouping column.}

\item{x}{Time (x-axis) variable. Accepts \code{Date}, \code{POSIXct}, numeric
years, or any value coercible to a date. (Unquoted.)}

\item{y}{Numeric measurement (y-axis) variable. (Unquoted.)}

\item{series}{Optional variable for plotting multiple lines per group
(e.g. a category). (Unquoted.)}

\item{.exclude}{Columns to exclude from becoming grouping dropdowns.
(Unquoted, tidy-select.)}

\item{.title}{Optional title string shown in the app header and in
downloaded figures. The group combination (\verb{a · b · c}) is always shown
separately and is not affected.}

\item{.y_min}{Optional numeric. Pre-fills the Y min input, fixing the
lower bound of the y-axis globally across all groups. Leave \code{NULL} for
automatic scaling. Set to \code{0} to replicate the old "start at zero" behaviour.}

\item{.y_max}{Optional numeric. Pre-fills the Y max input, fixing the
upper bound of the y-axis globally across all groups. Leave \code{NULL} for
automatic scaling.}
}
\value{
A data frame of flagged group combinations (the key columns only),
or \code{NULL} if nothing was flagged. Returned invisibly when the app exits.
}
\description{
Launches a Shiny application for visually reviewing time-series data
group by group. All columns that are not \code{x}, \code{y}, \code{series}, or excluded
become grouping dimensions with dropdown selectors. Navigate between
groups with the arrow keys. Press \code{Space} to flag the current combination,
\code{R} to reset zoom, and \code{Q} to quit.
}
\details{
Requires the \code{shiny}, \code{ggplot2}, and \code{plotly} packages.
}
\examples{
\dontrun{
library(dplyr)

# Simple example with economics dataset:
flagged <- ggplot2::economics_long |>
  screen_timeseries(date, value, series = variable)

# With a grouping column:
df <- data.frame(
  year    = rep(2010:2020, 3),
  country = rep(c("DK", "SE", "NO"), each = 11),
  gdp     = rnorm(33, 300, 20)
)
flagged <- screen_timeseries(df, x = year, y = gdp)
}

}
