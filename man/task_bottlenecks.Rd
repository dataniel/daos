% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{task_bottlenecks}
\alias{task_bottlenecks}
\title{Bottlenecks: the tasks blocking the most others}
\usage{
task_bottlenecks(db)
}
\arguments{
\item{db}{Path to the task database, or an open DBI connection.}
}
\value{
A tibble with \code{id}, \code{key}, \code{description}, \code{project}, \code{assignee},
and \code{blocking} (the number of pending tasks waiting on it), most
blocking first. Empty when nothing is blocked.
}
\description{
The unfinished prerequisites that hold up the most downstream work --
the real bottlenecks. Each row is a pending task that one or more other
pending tasks depend on, ranked by how many it blocks, so a lead can see
which single task to unblock first.
}
\seealso{
\code{\link[=task_blockers]{task_blockers()}}, \code{\link[=task_projects]{task_projects()}}
}
