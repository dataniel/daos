% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_excel.R
\name{append_excel}
\alias{append_excel}
\title{Append sheets to an existing Excel file}
\usage{
append_excel(
  data,
  path,
  overwrite = FALSE,
  as_table = FALSE,
  freeze_header = TRUE,
  skip_fmt = NULL,
  detect_years = TRUE
)
}
\arguments{
\item{data}{A data frame or named list of data frames to add as new
sheets. Same naming defaults as in \code{\link[=write_excel]{write_excel()}}.}

\item{path}{Path to an existing \code{.xlsx} file.}

\item{overwrite}{If \code{FALSE} (default), aborts when a sheet of the same name
already exists in the workbook. Set to \code{TRUE} to replace it.}

\item{as_table}{If \code{TRUE}, data is inserted as an Excel ListObject (table
with filter arrows and banded rows). Default \code{FALSE}.}

\item{freeze_header}{If \code{TRUE} (default), freezes the first row.}

\item{skip_fmt}{Character vector of column names to exclude from the
\verb{#,##0} number format.}

\item{detect_years}{If \code{TRUE} (default), numeric columns where every
non-\code{NA} value is a whole number between 1800 and 2200 are excluded
from the \verb{#,##0} number format.}
}
\value{
\code{path}, invisibly.
}
\description{
Adds one or more sheets to an existing \code{.xlsx} workbook without touching
its other sheets. New sheets get the same formatting as
\code{\link[=write_excel]{write_excel()}}: bold frozen header, blank \code{NA} cells, and \verb{#,##0}
number format for large numeric columns (with automatic exclusion of
year-like columns).
}
\examples{
\dontrun{
write_excel(list(Hoved = mtcars), "out.xlsx")
append_excel(list(Bilag = iris), "out.xlsx")
}

}
\seealso{
\code{\link[=write_excel]{write_excel()}}
}
