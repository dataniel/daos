% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_cvr.R
\name{clean_cvr}
\alias{clean_cvr}
\title{Standardise CVR numbers}
\usage{
clean_cvr(x)
}
\arguments{
\item{x}{A character vector of CVR numbers. Numeric vectors are
converted with \code{as.character()} first.}
}
\value{
A character vector of the same length.
}
\description{
Cleans a vector of Danish CVR numbers to the canonical 8-digit form:
dashes and spaces are stripped (\code{"12 34 56 78"} -> \code{"12345678"}), and a
leading \code{"DK"} VAT prefix is removed (\code{"DK12345678"} -> \code{"12345678"}).
}
\details{
Unlike \code{\link[=clean_cpr]{clean_cpr()}}, seven-digit values are \emph{not} zero-padded:
a lost leading zero cannot be distinguished from a typo, and inventing
a digit would let a malformed number slip past downstream checks such
as a \code{cvr \%like\% "^\\\\d{8}$"} checkpoint. Values are returned cleaned
but otherwise untouched, so malformed numbers stay visibly malformed.
}
\examples{
clean_cvr(c("DK12345678", "12 34 56 78", "12345678", NA))

# Typical use: standardise the join key on both sides
# df  |> dplyr::mutate(cvr = clean_cvr(cvr))

}
\seealso{
\code{\link[=clean_cpr]{clean_cpr()}}
}
