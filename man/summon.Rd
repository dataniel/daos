% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summon.R
\name{summon}
\alias{summon}
\title{Retrieve objects matching a pattern from an environment}
\usage{
summon(pattern, .envir = parent.frame())
}
\arguments{
\item{pattern}{A single regular expression string.}

\item{.envir}{The environment to search. Default is the calling
environment.}
}
\value{
A named list of matching objects.
}
\description{
Searches an environment for objects whose names match a regular expression
and returns them as a named list. Useful for collecting a family of
similarly-named objects (e.g. \code{dat0} through \code{dat9}) after unpacking a
list with \code{\link[=unpack_files]{unpack_files()}}.
}
\examples{
dat1 <- data.frame(x = 1)
dat2 <- data.frame(x = 2)
dat3 <- data.frame(x = 3)
summon("^dat\\\\d+$")

}
\seealso{
\code{\link[=unpack_files]{unpack_files()}}, \code{\link[=split_by]{split_by()}}
}
