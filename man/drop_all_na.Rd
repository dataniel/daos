% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drop_all_na.R
\name{drop_all_na}
\alias{drop_all_na}
\title{Drop all-\code{NA} rows and/or columns}
\usage{
drop_all_na(data, which = c("rows", "cols"))
}
\arguments{
\item{data}{A data frame or tibble.}

\item{which}{Which dimension(s) to clean: \code{"rows"}, \code{"cols"}, or both
(the default). Partial matching is allowed.}
}
\value{
\code{data} with fully-\code{NA} rows and/or columns removed. The class of the
input (data frame or tibble) is preserved.
}
\description{
Removes rows and/or columns that are entirely \code{NA} -- handy for tidying up
data after a join or import has left empty rows or unused columns behind.
Unlike \code{tidyr::drop_na()}, which drops a row as soon as it contains a single
\code{NA}, this only drops rows (or columns) where \emph{every} value is \code{NA}.

Equivalent to \code{janitor::remove_empty(which = which, cutoff = 1)},
reimplemented here to keep the dependency footprint small.
}
\examples{
df <- tibble::tibble(
  a = c(1, NA, 3),
  b = c(NA, NA, NA),
  c = c("x", NA, "z")
)

drop_all_na(df)                  # drops column b and the all-NA row
drop_all_na(df, which = "rows")  # only the all-NA row
drop_all_na(df, which = "cols")  # only column b

}
