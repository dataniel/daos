% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{task_modify}
\alias{task_modify}
\title{Modify a task}
\usage{
task_modify(
  db,
  id,
  description = NULL,
  key = NULL,
  project = NULL,
  assignee = NULL,
  tags = NULL,
  priority = NULL,
  due = NULL,
  recur = NULL,
  depends = NULL
)
}
\arguments{
\item{db}{Path to the task database, or an open DBI connection.}

\item{id}{Task identifier: the integer id, the uuid, or the key.}

\item{description}{The task text (required).}

\item{key}{Optional user-chosen handle to reference the task by -- a
slug of lowercase letters/digits joined by single \code{-} or \verb{_} (e.g.
\code{"compile-accounts"}). Must be unique across the database, and is
accepted anywhere an \code{id} is. Lets a production script refer to a task
by a stable, readable name instead of a brittle integer. The numeric
\code{id} and \code{uuid} keep working regardless.}

\item{project}{Optional project name.}

\item{assignee}{Optional person the task is assigned to.}

\item{tags}{Optional character vector of tags.}

\item{priority}{Optional priority: \code{"H"}, \code{"M"}, or \code{"L"}.}

\item{due}{Optional due date (\code{Date} or \code{"YYYY-MM-DD"}).}

\item{recur}{Optional recurrence applied when the task is completed: an
integer number of days, or one of \code{"daily"}, \code{"weekly"},
\code{"biweekly"}, \code{"monthly"}, \code{"quarterly"}, \code{"semiannual"}, \code{"yearly"}
-- the cadences statistics production runs on. Needs a \code{due}.}

\item{depends}{Optional ids (integer, uuid, or key) this task depends on.}
}
\value{
\code{TRUE}, invisibly.
}
\description{
Updates the supplied fields. Pass \code{tags} to replace the task's tags.
}
\details{
Pass \code{key} to set or rename the task's key; pass \code{key = ""} to
remove it. A new key must still be unique. Pass \code{tags} or \code{depends} to
\emph{replace} the task's tags or its dependencies (use \code{character(0)} to
clear them); pass \code{recur = ""} to stop a task recurring.
}
