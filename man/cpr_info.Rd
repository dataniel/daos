% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pnr_info.R
\name{cpr_info}
\alias{cpr_info}
\title{Extract information from Danish CPR numbers}
\usage{
cpr_info(
  data,
  cpr_col,
  add = c("bday", "age", "sex", "pnum", "mod11", "valid"),
  ref_date = Sys.Date()
)
}
\arguments{
\item{data}{A tibble or data frame.}

\item{cpr_col}{Name of the CPR column (unquoted).}

\item{add}{Which info types to add. Either:
\itemize{
\item An \strong{unnamed} character vector, e.g. \code{c("bday", "age")} — uses the
type names as column names.
\item A \strong{named} character vector, e.g. \code{c(birth_date = "bday", years_old = "age")} — left-hand side becomes the column name,
right-hand side is the type.
Default: all six types.
}}

\item{ref_date}{Reference date for age calculation. Accepts a \code{Date}
object or an ISO-format string (\code{"YYYY-MM-DD"}). Default: \code{Sys.Date()}.}
}
\value{
The original data frame with the requested columns appended.
}
\description{
Vectorised extraction of birth date, age, sex, sequential number, and
validity indicators from Danish CPR (Civil Person Register) numbers.
Returns the original data frame with the requested columns appended.
}
\details{
\strong{Supported info types} (\code{add} values):\tabular{lll}{
   Type \tab Output \tab Description \cr
   \code{"bday"} \tab Date \tab Date of birth \cr
   \code{"age"} \tab integer \tab Age in whole years at \code{ref_date} \cr
   \code{"sex"} \tab character \tab \code{"mand"} (male) or \code{"kvinde"} (female) \cr
   \code{"pnum"} \tab integer \tab Sequential (running) number (digits 7–10) \cr
   \code{"mod11"} \tab logical \tab Modulus-11 check (weights 4,3,2,7,6,5,4,3,2,1) \cr
   \code{"valid"} \tab logical \tab Format valid \emph{and} birth date parseable \cr
}


\strong{Century detection} follows the official CPR Register rules based on
digit 7 and the two-digit year component.

\strong{Input tolerance:} dashes and spaces are stripped automatically.
Nine-digit numbers are zero-padded on the left (recovering values that
lost a leading zero in Excel).
}
\examples{
df <- data.frame(
  pnr = c("1111111118", "111111-1118", "111111118"),
  stringsAsFactors = FALSE
)

# Default: add all columns
cpr_info(df, pnr)

# Custom column names:
cpr_info(df, pnr, add = c(birth_date = "bday", years_old = "age"))

# Unnamed — uses type names directly:
cpr_info(df, pnr, add = c("bday", "sex"))

}
