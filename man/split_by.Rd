% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_by.R
\name{split_by}
\alias{split_by}
\title{Split a data frame into a named list by grouping columns}
\usage{
split_by(data, ..., .sep = "_")
}
\arguments{
\item{data}{A data frame or tibble.}

\item{...}{Grouping columns (unquoted, tidy-select).}

\item{.sep}{Separator used to concatenate multiple group key values into a
single list name. Default: \code{"_"}.}
}
\value{
A named list of tibbles.
}
\description{
Groups a data frame and splits it, automatically naming each element of
the resulting list from the unique group key values. Unlike the bare
\code{\link[dplyr:group_split]{dplyr::group_split()}}, the returned list is always named.
}
\examples{
split_by(ggplot2::mpg, manufacturer)

# Multiple grouping columns:
parts <- split_by(ggplot2::mpg, manufacturer, cyl)
names(parts)

}
\seealso{
\code{\link[=summon]{summon()}} to retrieve objects matching a name pattern
}
