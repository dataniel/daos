% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{task_cycle}
\alias{task_cycle}
\title{Register a production cycle as a chain of steps}
\usage{
task_cycle(
  db,
  project,
  steps,
  keys = NULL,
  assignee = NULL,
  priority = NULL,
  due = NULL,
  recur = NULL
)
}
\arguments{
\item{db}{Path to the task database, or an open DBI connection.}

\item{project}{The production this cycle belongs to.}

\item{steps}{Character vector of step descriptions, in order.}

\item{keys}{Optional character vector of keys, one per step, so the
steps can be referenced by name from scripts.}

\item{assignee}{Optional person the task is assigned to.}

\item{priority}{Optional priority: \code{"H"}, \code{"M"}, or \code{"L"}.}

\item{due}{Optional deadline for the \emph{final} step (the release).}

\item{recur}{Optional recurrence for the final step, so completing the
cycle spawns the next release. See \code{\link[=task_add]{task_add()}} for the cadences.}
}
\value{
A tibble of the created tasks, in order.
}
\description{
Adds a sequence of tasks for one statistics production, wired up so each
step depends on the one before it. The result is a ready-to-run cycle:
\code{\link[=task_list]{task_list()}} reports only the first step as unblocked, and each later
step becomes ready as its predecessor is completed. A deadline (and an
optional recurrence, so the cycle's release date rolls forward) lands on
the final step.
}
\examples{
\dontrun{
task_cycle(db, "RS-2026",
  steps = c("Hent kilder", "Validér", "Kompilér", "Publicér"),
  keys  = c("hent", "valider", "kompiler", "publicer"),
  assignee = "pipeline", due = "2026-07-15", recur = "monthly")
}

}
\seealso{
\code{\link[=task_step]{task_step()}}, \code{\link[=task_require]{task_require()}}, \code{\link[=task_add]{task_add()}}
}
