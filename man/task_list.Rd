% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{task_list}
\alias{task_list}
\title{List tasks}
\usage{
task_list(
  db,
  status = "pending",
  project = NULL,
  assignee = NULL,
  tag = NULL,
  sort = c("urgency", "due", "entry", "project"),
  .only_uuid = NULL
)
}
\arguments{
\item{db}{Path to the task database, or an open DBI connection.}

\item{status}{Status filter: \code{"pending"} (default), \code{"completed"},
\code{"deleted"}, or \code{"all"}.}

\item{project}{Optional project filter.}

\item{assignee}{Optional person filter.}

\item{tag}{Optional tag filter (kept if the task carries the tag).}

\item{sort}{One of \code{"urgency"} (default), \code{"due"}, \code{"entry"},
\code{"project"}.}

\item{.only_uuid}{Internal: restrict to a single uuid.}
}
\value{
A tibble of tasks.
}
\description{
Returns tasks as a tibble, with tags aggregated, an annotation count, a
\code{blocked} flag (a dependency is still pending), and a simplified
taskwarrior \code{urgency} score. Sorted by urgency by default.
}
\examples{
\dontrun{
task_list("tasks.sqlite")
task_list("tasks.sqlite", project = "Q3", tag = "urgent")
}

}
\seealso{
\code{\link[=task_add]{task_add()}}, \code{\link[=task_projects]{task_projects()}}
}
