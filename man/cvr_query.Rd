% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cvr.R
\name{cvr_query}
\alias{cvr_query}
\title{Build an Elasticsearch query for CVR annual reports}
\usage{
cvr_query(cvr, enddate_from, enddate_to, size = 2999)
}
\arguments{
\item{cvr}{A character or numeric vector of CVR numbers (8 digits each).
At most 1000 per query, so split larger sets into batches.}

\item{enddate_from, enddate_to}{Accounting-period end-date range as
\code{"YYYY-MM-DD"} strings or \code{Date}s. Only reports whose period ends within
the range (inclusive) are matched.}

\item{size}{Maximum number of hits the API should return. Default \code{2999}.}
}
\value{
A nested list ready to be serialised as the JSON request body.
}
\description{
Constructs the query body used by \code{\link[=cvr_search]{cvr_search()}} to look up published
annual reports (offentliggoerelser) in Erhvervsstyrelsens distribution
service. Companies are matched by CVR number and accounting-period end
date. The function is pure: it only builds a nested list and performs no
network calls.
}
\details{
The full pipeline is:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cvr_query(cvr, from, to) |>
  cvr_search(contact = "you@example.com") |>
  cvr_hits() |>
  cvr_download("data/pdf")
}\if{html}{\out{</div>}}
}
\examples{
query <- cvr_query("12345678", "2024-01-01", "2024-12-31")
str(query, max.level = 3)

}
\seealso{
\code{\link[=cvr_search]{cvr_search()}}, \code{\link[=cvr_hits]{cvr_hits()}}, \code{\link[=cvr_download]{cvr_download()}}

Other cvr:
\code{\link[=cvr_download]{cvr_download()}},
\code{\link[=cvr_hits]{cvr_hits()}},
\code{\link[=cvr_search]{cvr_search()}}
}
\concept{cvr}
