% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{task_require}
\alias{task_require}
\title{Require tasks to have a given status}
\usage{
task_require(db, id, status = "completed")
}
\arguments{
\item{db}{Path to the task database, or an open DBI connection.}

\item{id}{One or more task ids (the small integers).}

\item{status}{Allowed status values; each task must be in one of them.}
}
\value{
\code{id}, invisibly.
}
\description{
Aborts unless every task in \code{id} has one of the statuses in \code{status}
(\code{"completed"} by default). This is a lightweight gate for coordinating a
production script: call it before a step that depends on earlier ones
being done. It only reads the task database and never touches your data,
so it belongs beside the analysis rather than inside it.
}
\examples{
\dontrun{
task_require(db, 1)            # stop unless task 1 is done
task_require(db, c(1, 2))      # ... or several upstream steps
}

}
\seealso{
\code{\link[=task_get]{task_get()}}, \code{\link[=task_done]{task_done()}}
}
