% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/size_env.R
\name{size_env}
\alias{size_env}
\title{Show object sizes in an environment}
\usage{
size_env(.envir = parent.frame(), n = NULL)
}
\arguments{
\item{.envir}{The environment to inspect. Default is the calling
environment.}

\item{n}{Optional integer. If supplied, only the \code{n} largest objects are
returned.}
}
\value{
A tibble with columns \code{name} (character), \code{size} (numeric, bytes),
and \code{pretty} (character, human-readable). Returns \code{NULL} invisibly if the
environment is empty.
}
\description{
Lists all objects in an environment sorted by size (descending), with
both raw byte counts and human-readable representations.
}
\examples{
x <- 1:1e6
y <- letters
size_env()       # all objects
size_env(n = 1)  # only the largest

}
