% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_access.R
\name{read_access}
\alias{read_access}
\title{Read data from a Microsoft Access database}
\usage{
read_access(path, sql, verbosity = c("compact", "full", "quiet"))
}
\arguments{
\item{path}{Path to the Access database file. Can be a string or an
\code{\link[fs:path]{fs::path}} object. Both \code{.mdb} and \code{.accdb} files are supported.}

\item{sql}{A SQL query string to execute against the database.}

\item{verbosity}{Level of status output. One of:
\itemize{
\item \code{"full"} – header, per-step spinners, and summary. Best for interactive
single-file use.
\item \code{"compact"} – a single-line summary per file. Best when iterating over
many databases. This is the default.
\item \code{"quiet"} – no output.
}}
}
\value{
A \code{\link[tibble:tibble]{tibble}} containing the query result.
}
\description{
Connects to a Microsoft Access database (\code{.mdb} or \code{.accdb}) via ODBC,
executes a SQL query, and returns the result as a tibble.
}
\examples{
\dontrun{
# single file – use full output
data <- read_access("sales.mdb", "SELECT * FROM Customers",
                    verbosity = "full")

# many files – compact output is the default
files <- fs::dir_ls("data", glob = "*.mdb")
all_data <- files |>
  purrr::map(\(f) read_access(f, "SELECT * FROM Sales"))
}

}
