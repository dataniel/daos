% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statbank.R
\name{statbank_tables}
\alias{statbank_tables}
\title{List every table in a statbank}
\usage{
statbank_tables(lang = NULL, bank = "gl", refresh = FALSE)
}
\arguments{
\item{lang}{Language of titles and labels, or \code{NULL} (default) for the
bank's own default. Greenland offers \code{"da"}, \code{"kl"}, \code{"en"}; the
Faroe Islands offer \code{"fo"}, \code{"en"}.}

\item{bank}{Which statbank: \code{"gl"} (Greenland, the default) or \code{"fo"}
(the Faroe Islands).}

\item{refresh}{If \code{TRUE}, ignore the session cache and walk the tree
again. Default \code{FALSE}.}
}
\value{
A tibble with columns \code{id}, \code{title}, \code{path} (the folder the
table sits in), and \code{updated}.
}
\description{
Walks the whole table tree once and returns all tables with their
paths and titles. The walk takes a number of small requests (one per
folder), so the result is cached for the rest of the session; pass
\code{refresh = TRUE} to fetch it again.
}
\examples{
\dontrun{
tables <- statbank_tables()
}

}
\seealso{
\code{\link[=statbank_search]{statbank_search()}}, \code{\link[=statbank_meta]{statbank_meta()}}
}
