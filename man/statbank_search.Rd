% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statbank.R
\name{statbank_search}
\alias{statbank_search}
\title{Search statbank tables by title}
\usage{
statbank_search(text, lang = NULL, bank = "gl", refresh = FALSE)
}
\arguments{
\item{text}{Search string, interpreted as a regular expression.}

\item{lang}{Language of titles and labels, or \code{NULL} (default) for the
bank's own default. Greenland offers \code{"da"}, \code{"kl"}, \code{"en"}; the
Faroe Islands offer \code{"fo"}, \code{"en"}.}

\item{bank}{Which statbank: \code{"gl"} (Greenland, the default) or \code{"fo"}
(the Faroe Islands). It may also be a full base URL (everything
before the table path, with language and database node already in
it, e.g. \code{"https://bank.stat.gl/api/v1/da/Greenland"}), to reach any
PXWeb v1 endpoint; \code{lang} is then ignored.}

\item{refresh}{If \code{TRUE}, ignore the session cache and walk the tree
again. Default \code{FALSE}.}
}
\value{
A tibble with the matching rows from \code{\link[=statbank_tables]{statbank_tables()}}.
}
\description{
Filters the full table list (see \code{\link[=statbank_tables]{statbank_tables()}}) on a search
string, matched case-insensitively against titles and table ids. The
first call walks the table tree; later calls use the session cache.
}
\examples{
\dontrun{
statbank_search("befolkning")
statbank_search(" wages", bank = "fo")
}

}
\seealso{
\code{\link[=statbank_tables]{statbank_tables()}}, \code{\link[=statbank_meta]{statbank_meta()}}
}
