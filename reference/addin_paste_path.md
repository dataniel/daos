# RStudio addin: paste path from clipboard

Reads a Windows path from the clipboard, replaces backslashes with
forward slashes, and inserts it as a quoted R string at the cursor
position.

## Usage

``` r
addin_paste_path()
```
